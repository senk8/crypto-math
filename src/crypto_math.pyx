# cython: profile=True

# For example, if you want to use math_util from an extension, you don't need to import it,
# but include it by pasting the contents of the file as it is, just like in C.

include "helper.pyx"
include "util.pyx"
include "galois_field.pyx"
include "poly_ring.pyx"
include "extension.pyx"
include "euclidean.pyx"
include "comb.pyx"
include "prime.pyx"
include "gmp.pyx"
